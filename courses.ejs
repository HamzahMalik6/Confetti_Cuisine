<div class="container">
    <div class="row">
        <div class="col-sm-6 mx-auto">
            <img src="images/fruit.jpg">
        </div>
        <div class="col-sm-12">
            <h1 id="title" class="my-4 text-center">
                Learn to cook cutting edge food
            </h1>

            <h3 class="mb-3">Our Courses</h3>
            <% offeredCourses.forEach(course=> { %>
                <h5>
                    <%= course.title %>
                </h5>

                <p>
                    <%= course.cost %>
                </p>
            <% }); %>
        </div>
    </div>
</div>